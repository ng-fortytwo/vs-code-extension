export interface EnableBarrelTemplates {
  header: boolean;
  footer: boolean;
}

export interface EnableBarrelMenus {
  createBarrel: boolean;
  createDirectoryBarrel: boolean;
  createFileBarrel: boolean;
}
